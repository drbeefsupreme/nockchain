[project]
name = "pma-bench-sync"
version = "0.1.0"
description = "Operator-oriented PMA bench sync tool"
requires-python = ">=3.11"
dependencies = [
  "tomlkit>=0.13,<1",
]

[tool.uv]
package = false
